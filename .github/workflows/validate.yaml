name: Validate

on:
  workflow_call:
    inputs:
      sdk:
        description: 'Versions of the SDK'
        required: true
        type: string[]

      target:
        description: 'Solution to Validate'
        required: true
        type: string

      configuration:
        description: 'Configuration of the Build'
        required: false
        default: 'Release'
        type: string

jobs:
  check:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.sdk }}

      - run: dotnet restore --locked-mode ${{ inputs.target }}

      - run: dotnet test --no-build ${{ inputs.target }}
